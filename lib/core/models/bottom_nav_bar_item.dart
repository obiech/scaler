class BottomBarAppItem {
  const BottomBarAppItem({
    required this.icon,
    required this.text,
  });

  final String icon; // Path to the icon asset.
  final String text; // Label for the tab.
}
